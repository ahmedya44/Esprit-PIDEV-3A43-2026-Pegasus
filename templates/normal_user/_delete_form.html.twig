<form method="post" action="{{ path('app_normal_user_delete', {'id': normal_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ normal_user.id) }}">
    <button class="btn">Delete</button>
</form>
