<form method="post" action="{{ path('app_categorie_delete', {'id': categorie.id}) }}" class="d-inline" data-confirm-message="Delete this category?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn">Delete</button>
</form>
